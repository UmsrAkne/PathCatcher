<Window
    x:Class="PathCatcher.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:PathCatcher.Models"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:system="clr-namespace:System;assembly=System.Runtime"
    xmlns:utils="clr-namespace:PathCatcher.Utils"
    xmlns:viewModels="clr-namespace:PathCatcher.ViewModels"
    Title="{Binding Title}"
    Width="800"
    Height="350"
    d:DataContext="{d:DesignInstance viewModels:MainWindowViewModel}"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="{DynamicResource WindowBackgroundBrush}"
    Foreground="{DynamicResource ForegroundBrush}"
    mc:Ignorable="d">

    <Window.Resources>
        <!-- Converters -->
        <utils:LeftEllipsisConverter x:Key="LeftEllipsisConverter" />

        <!-- Dark theme brushes -->
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#1E1E1E" />
        <SolidColorBrush x:Key="SurfaceBrush" Color="#2B2B2B" />
        <SolidColorBrush x:Key="ForegroundBrush" Color="#EAEAEA" />
        <SolidColorBrush x:Key="SubtleForegroundBrush" Color="#C4C4C4" />
        <SolidColorBrush x:Key="BorderBrushColor" Color="#3A3A3A" />
        <SolidColorBrush x:Key="AccentBrush" Color="#3A86FF" />
        <SolidColorBrush x:Key="SelectionBrush" Color="#3D5A80" />

        <!-- Base control foreground -->
        <Style TargetType="Control">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
        </Style>

        <!-- TextBlock inherits foreground -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
        </Style>

        <!-- TabControl and TabItem -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushColor}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="#1F1F1F" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushColor}" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border
                            x:Name="Border"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1,1,1,0">

                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                ContentSource="Header" />
                        </Border>

                        <ControlTemplate.Triggers>

                            <!-- 選択中 -->
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource SurfaceBrush}" />
                            </Trigger>

                            <!-- マウスオーバー -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#303030" />
                            </Trigger>

                            <!-- 無効状態 -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListBox and items -->
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushColor}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
            <Setter Property="Padding" Value="6,3" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#333333" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource SelectionBrush}" />
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1F1F1F" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushColor}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="CaretBrush" Value="{DynamicResource ForegroundBrush}" />
            <Setter Property="SelectionBrush" Value="{DynamicResource AccentBrush}" />
        </Style>

        <!-- Button -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3A3A3A" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushColor}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Cursor" Value="Hand" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A4A4A" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#2F2F2F" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Foreground" Value="#777777" />
                    <Setter Property="Background" Value="#2A2A2A" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Border -->
        <Style TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrushColor}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <!-- ContentControl -->
        <Style TargetType="ContentControl">
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
        </Style>
    </Window.Resources>

    <TabControl>
        <TabItem Header="Directories">

            <Grid Background="{DynamicResource SurfaceBrush}">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <ListBox Grid.Row="0" ItemsSource="{Binding DirectoryPaths}">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type system:String}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Converter={StaticResource LeftEllipsisConverter}, ConverterParameter=70}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>


                <StackPanel Grid.Row="1">
                    <TextBlock
                        Margin="5"
                        Foreground="{DynamicResource SubtleForegroundBrush}"
                        Text="Enter the full path of the directory to monitor :" />

                    <TextBox Margin="5,0" Text="{Binding PendingPath, UpdateSourceTrigger=PropertyChanged}" />

                    <StackPanel
                        Margin="5"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal">

                        <Button
                            Padding="20,3"
                            Command="{Binding AddDPathCommand}"
                            Content="Add directory" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </TabItem>

        <TabItem Header="Histories" IsSelected="True">
            <Grid Background="{DynamicResource SurfaceBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="0.75*" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0">
                    <ListBox
                        x:Name="HistoryListBox"
                        ItemsSource="{Binding Histories}"
                        SelectedItem="{Binding SelectedCopyHistory}">

                        <ListBox.InputBindings>
                            <KeyBinding
                                Key="C"
                                Command="{Binding CopyFromHistoryCommand}"
                                CommandParameter="{Binding ElementName=HistoryListBox, Path=SelectedItem}"
                                Modifiers="Control" />
                        </ListBox.InputBindings>

                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:CopyHistory}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Margin="10,0" Text="{Binding DateTime, StringFormat=HH:mm:ss}" />
                                    <TextBlock Text="{Binding FilePath, Converter={StaticResource LeftEllipsisConverter}, ConverterParameter=50}" />
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>
                <ContentControl Grid.Column="1">
                    <ContentControl.Style>
                        <Style TargetType="ContentControl">
                            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedCopyHistory.ContentType}">
                                    <DataTrigger.Value>
                                        <models:ContentType>Text</models:ContentType>
                                    </DataTrigger.Value>
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <TextBox Text="{Binding SelectedCopyHistory.FilePath, Converter={StaticResource TextFileContentConverter}}" />
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>

                                <DataTrigger Binding="{Binding SelectedCopyHistory.ContentType}">
                                    <DataTrigger.Value>
                                        <models:ContentType>Image</models:ContentType>
                                    </DataTrigger.Value>
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <Image Source="{Binding SelectedCopyHistory.FilePath, Converter={StaticResource ImagePathToBitmapSourceConverter}}" />
                                        </Setter.Value>
                                    </Setter>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ContentControl.Style>
                </ContentControl>
            </Grid>
        </TabItem>
    </TabControl>

</Window>